[
    {
        "id": 1,
        "title": "Vowel Count",
        "description": "Return the number (count) of vowels in the given string. We will consider a, e, i, o, u as vowels \nfor this Kata (but not y). The input string will only consist of lower case letters and/or spaces.",
        "code": "function getCount(str) {\n  let counter = 0;\n  let vowels = 'aeiou';\n  for (let i = 0; i < str.length; i++) {\n    if (vowels.includes(str[i])) {\n      counter++;\n    }\n  }\n  return counter;\n}"
    },
    {
        "id": 2,
        "title": "Максимум в массиве",
        "description": "Найди максимальное число в массиве.",
        "code": "const max = Math.max(...arr);"
    }
]